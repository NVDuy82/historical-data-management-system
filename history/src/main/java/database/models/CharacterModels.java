package database.models;

import java.util.ArrayList;

public class CharacterModels extends Models {
	public CharacterModels(String name, String id, String info, ArrayList<String> connect) {
		super(name, id, info, connect);
	}
}
